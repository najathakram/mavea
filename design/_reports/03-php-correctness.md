All 21 PHP files lint clean (`php -l` in `slk-wp`, PHP 8.3). Every WooCommerce hook used is real — I verified each against WooCommerce 11.0.1 source in the container (`woocommerce_get_country_locale` @ class-wc-countries.php:899, `default_checkout_billing_country` @ class-wc-checkout.php:1522, `woocommerce_form_field_args` @ wc-template-functions.php:3353, `woocommerce_dropdown_variation_attribute_options_html` @ :3774, loop/single priorities @ wc-template-hooks.php:39-182, `wc_empty_cart_message` @ :249). No invented hooks. No HPOS violations — `get_post_meta`/`update_post_meta` appear only inside comments. `class-slk-money.php`, `class-slk-phone.php`, `class-slk-shipping-method.php`, `class-slk-order-admin.php`, `content-product.php`, `content-single-product.php` are clean.

Defects, most severe first:

**1. `C:/ClaudeCode/sldress/local/themes/slk-child/woocommerce/checkout/form-checkout.php:55-58` — panel "2 · Where" contains no address fields.**
`do_action( 'woocommerce_checkout_billing' )` (line 52, panel 1) runs `checkout/form-billing.php`, which loops **the entire** `get_checkout_fields('billing')` array. `billing_address_1`, `billing_landmark`, `billing_city`, `billing_state`, `billing_postcode` are all defined in the *billing* group by `class-slk-checkout-fields.php:117-165`, so they render inside panel "1 · You". `woocommerce_checkout_shipping` (line 57, panel 2) runs `form-shipping.php`, which under `ship_to_billing_address_only`/no-shipping-address renders only the "Ship to a different address?" checkbox and the Delivery-notes textarea. Result: panel 1 = phone, name, email, country, street, landmark, city, district, postcode; panel 2 = a checkbox and a notes box. The priorities 10–80 only sort *within* panel 1. Fix: add a `woocommerce/checkout/form-billing.php` override that keeps the full field loop and both `woocommerce_before/after_checkout_billing_form` hooks, but emits `</div><div class="slk-panel slk-panel--lifted slk-checkout__panel" id="slk-panel-where"><div class="slk-eyebrow slk-checkout__panel-label">2 · Where</div>` inside the loop at the first field whose `priority >= 40`; then delete the `#slk-panel-where` wrapper from form-checkout.php lines 55-58 and move `do_action('woocommerce_checkout_shipping')` to just before that panel's closing `</div>`.

**2. `C:/ClaudeCode/sldress/local/themes/slk-child/inc/checkout-view.php:133` — the checkout JS is never printed.**
`wp_add_inline_script( 'slk-child', ... )` targets a handle that exists only in the **styles** registry. `WP_Scripts::add_data()` returns `false` for an unregistered handle, silently. Proven at runtime in the container: `slk-child registered as SCRIPT: NO / STYLE: YES / add_inline_script result: false`. `slk_checkout_view_js()` never runs, so `#slk-payment-slot` stays permanently empty and the "3 · Paying" panel renders as an empty glass box (the `<noscript>` fallback at form-checkout.php:68 does not display, because JS *is* enabled). Fix: mirror `inc/pdp.php:382` — before line 130, add `wp_register_script( 'slk-checkout-view', false, array( 'jquery' ), null, true ); wp_enqueue_script( 'slk-checkout-view' );` and change line 133 to `wp_add_inline_script( 'slk-checkout-view', slk_checkout_view_js() );`.

**3. `C:/ClaudeCode/sldress/local/themes/slk-child/inc/cart.php:346-351` — the quantity stepper never updates the cart.**
`form.requestSubmit()` is called with no submitter argument, so no submit button's `name=value` pair is posted. `WC_Form_Handler::update_cart_action()` (class-wc-form-handler.php, first statement) early-returns unless `$_REQUEST` contains one of `apply_coupon|remove_coupon|remove_item|undo_item|update_cart|proceed`. The POST is therefore a no-op: the page reloads with the old quantity, the stepper appears broken. Fix: replace `form.requestSubmit();` with `var submitter = form.querySelector( 'button[name="update_cart"]' ); if ( submitter ) { form.requestSubmit( submitter ); } else { form.submit(); }`.

**4. `C:/ClaudeCode/sldress/local/themes/slk-child/inc/pdp.php:242` — fatal error when WooCommerce is deactivated.**
`is_product()` is called unguarded inside a `wp_enqueue_scripts` callback. `functions.php:96-112` loads every `inc/*.php` unconditionally on `after_setup_theme`, so with WooCommerce off this is `Fatal error: Uncaught Error: Call to undefined function is_product()` on every front-end request — a white screen the merchant cannot recover from via wp-admin plugin toggling. `inc/cart.php:209`, `inc/checkout-view.php:124` and `inc/shop.php:297` all guard correctly; pdp.php is the only one that does not. Fix: change line 242 to `if ( ! function_exists( 'is_product' ) || ! is_product() ) { return; }`.

**5. `C:/ClaudeCode/sldress/local/themes/slk-child/inc/checkout-view.php:276-284` — duplicated payment-method radios after any AJAX refresh (latent behind #2).**
`WC_AJAX::update_order_review` returns the fragment key `.woocommerce-checkout-payment` (class-wc-ajax.php:496), which jQuery `replaceWith`s the whole `#payment` div — including a freshly rendered `ul.payment_methods`. `slkArrangeCheckout` appends that new list to the slot but never removes the previous one already sitting there, so after the first payment-method change (which `assets/js/checkout.js` itself triggers on every change) the page carries two `ul.payment_methods`, duplicate `id="payment_method_cod"` values, and two radio sets sharing `name="payment_method"` — the later one in DOM order wins on submit. Fix: inside `slkArrangeCheckout`, before the append, drop any stale copy: `Array.prototype.forEach.call( slot.querySelectorAll( 'ul.payment_methods' ), function ( old ) { if ( old !== methods ) { old.remove(); } } );`.

**6. `C:/ClaudeCode/sldress/local/themes/slk-child/woocommerce/checkout/form-checkout.php:42,98` — order review and the Place-order button are inside the `get_checkout_fields()` conditional.**
In the reference template (`design/_reference/woocommerce-templates/checkout/form-checkout.php:50-62`) `endif;` closes at line 50 and `woocommerce_checkout_before_order_review_heading`, `#order_review`, `woocommerce_checkout_order_review` and `woocommerce_checkout_after_order_review` all sit *outside* it. The override moves them inside (lines 78-90 precede `endif` at line 98). If any plugin filters `woocommerce_checkout_fields` to empty — or a store configuration produces no fields — the reference still renders the totals and `#place_order`; this override renders an empty `<form>` with no way to submit, and no error. Fix: move `<?php endif; ?>` from line 98 to immediately after line 74's `</div>` closing `.slk-checkout__fields`, and close `#customer_details` / open `.slk-checkout__aside` outside the conditional, matching the reference nesting.

**7. `C:/ClaudeCode/sldress/local/themes/slk-child/inc/shop.php:272-283` — variable-product price ranges are silently replaced with the minimum price.**
```php
return wc_price( wc_get_price_to_display( $product ) );
```
`wc_get_price_to_display()` defaults to `$product->get_price()`, which for `WC_Product_Variable` is the *minimum* variation price, and `WC_Product_Variable::is_on_sale()` is true if **any** variation is on sale. So a dress listed "Rs. 12,500 – Rs. 15,900" collapses to "Rs. 12,500" as soon as one variation carries a sale price — an understated price on the card, the PDP, the Store API and the admin product list (the filter has no `is_admin()` guard). Fix: guard on product type and strip only the `<del>` half —
```php
if ( ! $product instanceof WC_Product || ! $product->is_on_sale() || $product->is_type( array( 'variable', 'grouped' ) ) ) {
    return $price_html;
}
return preg_replace( '#<del\b[^>]*>.*?</del>#is', '', $price_html );
```

**8. `C:/ClaudeCode/sldress/local/themes/slk-child/inc/checkout-view.php:36-41` — `slk_mask_phone()` produces the wrong string and leaks more digits than it hides.**
Verified by execution: `+94771234567` → `+94771 23 ··· ·567`, `0771234567` → `+0771 23 ··· ·567`, a 6-digit input → `+ 12 ··· ·456` (empty prefix, stray space after `+`). The docblock at line 24 promises `+94 77 ··· ·567`. `$prefix` takes everything except the last six digits, and `$mid[0].$mid[1]` drops the third mid digit. This is what the shopper reads on the order-received screen (thankyou.php:105). Fix: replace lines 36-41 with
```php
$national = SLK_Phone::national_digits( $digits ); // or: substr( $digits, -9 )
if ( 9 !== strlen( $national ) ) { return ''; }
return '+94 ' . substr( $national, 0, 2 ) . ' ··· ·' . substr( $national, -3 );
```

**9. `C:/ClaudeCode/sldress/local/themes/slk-child/woocommerce/cart/cart.php:60,224,272` — three cart hooks now fire inside a `<ul>` instead of a `<tbody>`.**
`woocommerce_before_cart_contents`, `woocommerce_cart_contents` and `woocommerce_after_cart_contents` are documented `<tbody>` hooks; every plugin that uses them (bundles, gift wrap, free-gift notices, WooCommerce's own recurring-cart addons) echoes `<tr><td colspan="6">…</td></tr>`. Inside a `<ul>` the HTML parser foster-parents that markup out of the list or drops it, so those rows vanish. The file header at line 6 claims "All the original hooks/filters are preserved" — they fire, but in an invalid container. Fix: either wrap each of those three `do_action()` calls in `<table class="slk-cart-hook-shim" style="display:contents"><tbody>…</tbody></table>`, or capture with `ob_start()` and inject the stripped text into an `<li class="slk-cart-hook">`. At minimum correct the header claim so the next maintainer knows.

**10. `C:/ClaudeCode/sldress/local/plugins/slk-checkout/includes/class-slk-districts.php:141` — the LK locale is replaced, not merged, contradicting the file header.**
Line 8 states "nothing of core's is overridden". WooCommerce 11.0.1 ships an LK entry at `class-wc-countries.php:1592` (`'LK' => array( 'state' => array( 'required' => false, 'hidden' => true ) )`), and `$locale[ self::COUNTRY ] = array( … )` discards it wholesale. Overriding `state` is intended here, but any *future* LK key core adds (postcode format, address_1 label) is also silently dropped. Fix: `$locale[ self::COUNTRY ] = array_replace_recursive( isset( $locale[ self::COUNTRY ] ) && is_array( $locale[ self::COUNTRY ] ) ? $locale[ self::COUNTRY ] : array(), $slk_lk_locale );` and correct the header comment.

**11. `C:/ClaudeCode/sldress/local/themes/slk-child/inc/wordmark.php:190-201` — the `bloginfo` filter does not do what the docblock claims.**
`get_bloginfo()` applies the `bloginfo` filter **only** when `$filter === 'display'` (i.e. via `bloginfo()`), never for the default `get_bloginfo( 'name' )`. The document `<title>`, RSS feeds, and WooCommerce's email from-name all call `get_bloginfo('name')` raw, so they keep showing the WP option value, not the wordmark — the exact opposite of the comment at lines 187-188. Fix: use the option filter instead — `add_filter( 'option_blogname', static function ( $v ) { return is_admin() ? $v : slk_wordmark_text(); }, 20 );` (keep the `bloginfo` filter too if you want `bloginfo()` call sites covered).

**12. `C:/ClaudeCode/sldress/local/themes/slk-child/inc/checkout-view.php:80-100` — a global form-field filter with no scope guard, duplicating classes the plugin already set.**
`woocommerce_form_field_args` fires for **every** `woocommerce_form_field()` call site — My Account edit-address, login/register, order tracking, coupon forms, and admin-side gateway settings — not just the checkout, yet the CSS it feeds (`slk_checkout_view_css()`) is enqueued on `is_checkout()` only. It also re-adds `slk-field`/`slk-input`, which `class-slk-checkout-fields.php:57-58,73-74,108-110,129-130,146-147,152-164` already put on the same fields, producing `class="form-row-wide slk-field slk-field"` and `class="input-text slk-input slk-input"`. Fix: wrap the callback body in `if ( ! function_exists( 'is_checkout' ) || ! is_checkout() ) { return $args; }` and de-duplicate with `array_values( array_unique( array_merge( … ) ) )`.

**13. `C:/ClaudeCode/sldress/local/themes/slk-child/style.css:6` — text domain `slk` is declared but never loaded by the theme.**
There is no `load_child_theme_textdomain()` anywhere in the theme (grepped) and no `languages/` directory. Every `__( …, 'slk' )` in `inc/*.php` and `woocommerce/**/*.php` resolves only if `slk-checkout.php:94` happens to have loaded that domain from the *plugin's* `/languages` path — so theme strings can never be translated from the theme, and are untranslated entirely when the plugin is off. WP also expects a child theme's domain to match its slug. Fix: add to `functions.php` — `add_action( 'after_setup_theme', static function () { load_child_theme_textdomain( 'slk', get_stylesheet_directory() . '/languages' ); }, 6 );` and create `languages/`.

**14. `C:/ClaudeCode/sldress/local/themes/slk-child/inc/cart.php:121-127` — the empty-cart fallback rail can show catalog-hidden products.**
The recently-viewed branch (line 96) filters on `$product->is_visible()`; the popularity fallback checks only `$product->exists()`. A product with catalog visibility "Hidden" (or "Search results only") therefore appears in the "You looked at these" rail and links to a page the merchant deliberately unlisted. Fix: change line 124 to `if ( $product && $product->exists() && $product->is_visible() ) {`.